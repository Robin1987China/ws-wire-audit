# LBank 公开 API 契约问题清单

> 面向交易所工程团队的公开问题清单。只涉及公开文档与公开行情端点，不含任何非公开接口。

**文档性质**：面向交易所工程团队与第三方集成者的公开问题清单。
**适用范围**：LBank 现货与合约的**公开** API（公开行情端点与公开技术文档）。本清单**不涉及**签名、账户、交易、资金类接口。
**观察时间**：2026-09-21（UTC+8）。测量方法与环境见附录 A。
**证据等级**：**[A]** 官方文档原文 / 官方端点实测（可复现）｜**[B]** 第三方公开来源（开源库源码、机构级连接器文档）｜**[C]** 自媒体与聚合站线索（本次**未使用**）。
**数字可回源**：本文每个数字都写明取得方式；读者无法独立复现的引用，已逐处标注「**待公开化**」并汇总于附录 C。

---

## 一、摘要

**编号条目总数：23 条。**

| 定性 | 条数 | 编号 |
|---|---|---|
| 文档与实现不符 | 7 | L-01、L-02、L-03、L-05、L-06、L-07、L-11 |
| 文档缺失（内容未写） | 10 | L-04、L-08、L-09、L-10、L-13、L-15、L-16、L-17、L-19、L-20 |
| 可达性 / 仓库运维（非契约） | 3 | L-14、L-22、L-23 |
| 行为观测（本次观测，n=1） | 1 | L-21 |
| 未判定（本清单不下结论） | 1 | L-18 |
| 已关闭（非缺陷，保留作背景） | 1 | L-12 |
| **合计** | **23** | |

> **计数口径**：本清单 v1.1 稿的标题曾写「共 24 条」，按编号逐条点数为 **23 条**。本版以逐条点数为准。

**建议优先处理的 4 条（L-08、L-13、L-01、L-02，分 3 组）**

1. **L-08｜日/周/月 K 线桶边界按 UTC+8 对齐，但返回的时间戳是 UTC 纪元秒，文档未声明边界时区。**
   这类问题不返回任何错误：按错误时区解释 K 线只会让下游静默算错 8 小时，且在盘中不易察觉。
2. **L-13｜限流章节仅两条全局限额，缺少分端点配额、限流维度、`429`/`Retry-After` 语义与退避建议。**
   客户端难以判断边界，也难以实现与官方节奏一致的退避；对做市/信号类客户端属可观测性缺口。
3. **L-01 / L-02｜响应信封的 `result` 字段类型在同一套 API 内不一致；部分端点 `msg` 为 `null`。**
   强类型客户端会在部分端点解析失败，或以 `msg` 判成功而误判。

其余 **19 条**按性质分布为：文档与实现不符 5 条（L-03、L-05、L-06、L-07、L-11）、文档缺失 8 条（L-04、L-09、L-10、L-15、L-16、L-17、L-19、L-20）、可达性与仓库运维 3 条（L-14、L-22、L-23）、行为观测 1 条（L-21）、未判定 1 条（L-18）、已关闭 1 条（L-12）。**本清单不主张以上问题同等重要，也不对贵团队的整体质量作评价**；每条给出「对开发者意味着什么」，优先级由贵方判断。

> **计数自洽**：4（优先）+ 19（其余）= **23**，与上表逐条点数一致；每一条只出现在一个类别里（本版已消除 v1.1 稿中「L-12 重复计入、L-09/L-10/L-11 未列入」的算术缺口）。

---

## 二、如何验证我们说的

本节的目的：让读者不必相信本文，而是能自己复现。

### 2.1 复现前提

- 本文涉及的全部端点均为**公开行情端点**：无需 `api_key`、无需 `sign`、无需注册、无需开户。文档原文：「To protect API communication from unauthorized change, all **non-public** API calls are required to be signed.」
- 任意能访问 `api.lbank.info` 的网络出口 + `curl`（或任意 HTTP 客户端）即可复现 REST 部分；WebSocket 部分用任意符合 RFC6455 的客户端。
- 文档要求以**非中国大陆出口**发起 API 调用（原文：*"Please initiate API calls with non-China IP"*）。不同出口地区会改变可达性结论（见 L-23），但不影响契约类结论。
- 官方文档有两个入口：`https://www.lbank.info/docs/index.html`（现货）与 `https://www.lbank.info/docs/contract.html`（合约）。本文的文档原文引用一律给出**小节名**，可直接在该页检索。

### 2.2 三类证据，各自的验证方式

| 等级 | 含义 | 读者如何验证 |
|---|---|---|
| **A**（文档原文） | 我们直接引用官方文档的句子/表格 | 打开对应小节，逐字比对。本文引文均以「」或原文句标注，并给出小节名 |
| **A**（端点实测） | 我们向公开行情端点发出**只读**请求 | 复制本文的 `curl`，原样执行，比对响应字段 |
| **B**（第三方公开来源） | 开源库源码、机构级连接器文档 | 到该开源项目仓库按**符号名**检索（行号随版本变动，故本文以符号名定位） |

### 2.3 复现纪律（我们遵守，也建议复现者遵守）

- 只读 GET/POST **公开**端点；无签名、无账户、无交易、无资金操作；
- 单次、顺序执行，间隔 ≥1.2 秒；**无并发、无重试、无扫描、无压测**；
- 不绕过任何访问控制（L-23 的地点限制页即为一例：我们改用官方另一入口，未做任何绕过）。

### 2.4 如果你的结果与我们不同

- **行情数值会随时间变化**，因此 L-01/L-02/L-03/L-13/L-15~L-20 这类结论与具体数值无关，只看字段类型、字段存在性与文档表述；
- **时区类结论（L-08/L-09/L-10）只用桶边界判断**，与具体价格无关：只需比较「请求时间」与「返回首桶时间戳」的换算关系；
- 地区出口不同会改变 L-23 的结论；
- 若某条已被修复，欢迎告知，我们会在复核记录中标注为已关闭。

### 2.5 我们可以提供什么

- 任何一条的**原始 HTTP/WS 响应全文**（含握手头），可按条目分别提供；
- 上述 20 次访问的完整请求清单（附录 A 已列出端点与参数）；
- 逐字文档副本中我们据以定位的行号（对外化的载体格式**待公开化**，见附录 C）。

---

## 三、问题清单

> 每条结构：**问题 → 复现步骤 → 观察到的行为 → 影响 → 建议修复**（附证据等级）。
> 「本次观测」= 单次观测，不作为普遍性定性；「未判定」项的判定方法见 §五。

### A 组｜响应信封

#### L-01 `result` 字段类型在同一套公开 API 内不一致

**证据等级**：A（官方文档原文 + 官方端点实测）

**问题**
官方文档 Response Format 表将 `result` 字段的类型列为 `String`。实测同一域名下两种类型并存：部分端点返回 JSON 布尔，部分端点返回 JSON 字符串。

**复现步骤**
1. `curl "https://api.lbank.info/v2/supplement/ticker/price.do?symbol=btc_usdt"`
2. `curl "https://api.lbank.info/v2/ticker/24hr.do?symbol=btc_usdt"`
3. `curl "https://api.lbank.info/v2/depth.do?symbol=btc_usdt&size=5"`
4. 比较三个响应根对象中 `result` 字段的 JSON 类型（布尔还是字符串）

**观察到的行为（本次观测）**
- 步骤 1 → `{"result": true, ...}`：JSON **布尔**；
- 步骤 2、3 → `{"result": "true", ...}`：JSON **字符串**；
- `supplement/ticker/bookTicker.do`、`kline.do` 同步骤 2（字符串形式）；
- 官方文档 Response Format 表所列类型为 `String`。

**影响**
强类型客户端（Go/Rust/Java 的 struct 绑定、Pydantic/dataclass 校验）会在**部分端点**解析失败，或被迫为 `result` 写双类型兼容层；若下游用 `if result:` 判断，字符串 `"true"` 会被静默视为真，错误被吞掉。

**建议修复**
统一为 JSON 布尔。如需向后兼容过渡，可在 Response Format 表中逐端点列出实际类型，并在 changelog 标注变更计划。

---

#### L-02 部分端点的 `msg` 字段为 `null`

**证据等级**：A（官方文档原文 + 官方端点实测）

**问题**
官方文档 Response Format 表声明响应含 `msg` 字段；实测部分公开端点的 `msg` 为 `null` 或缺失。

**复现步骤**
1. `curl "https://api.lbank.info/v2/supplement/ticker/price.do?symbol=btc_usdt"`
2. `curl -X POST "https://api.lbank.info/v2/supplement/system_ping.do"`
3. 查看两个响应根对象的 `msg` 字段

**观察到的行为（本次观测）**
- 两个请求均为 HTTP 200、`error_code: 0`，但 `"msg": null`；
- 官方文档 Response Format 表所列该字段为存在字段。

**影响**
以 `msg == "Success"` 判断成功的客户端会把成功响应判为失败；反向写法（`if msg: 视为失败`）也可能漏判。

**建议修复**
`msg` 恒返回非空字符串（成功时 `"Success"`）；或在文档中明确「`msg` 可能为 null，请以 `error_code` 为准」，并把这一约定写入各语言 SDK 的示例。

---

#### L-03 错误码 10003 的文案与文档不一致

**证据等级**：A（官方文档原文 + 官方端点实测）

**问题**
官方文档错误码表将 `10003` 的文案列为 `Invalid parameter`；实际返回的 `msg` 为 `Illegal parameter`。

**复现步骤**
```
curl "https://api.lbank.info/v2/kline.do?symbol=btc_usdt&size=3&type=minute1"
```
（故意不带 `time`：`time` 为文档标注的必填参数）

**观察到的行为（本次观测）**
HTTP 200，响应体 `{"result":"false","error_code":10003,"msg":"Illegal parameter"}`；而文档错误码表中 10003 的文案为 `Invalid parameter`。

**影响**
按文档文案做的字符串匹配（日志告警、自动重试规则、错误分类映射）会失效；以文案为键的 SDK 错误映射会错分类。

**建议修复**
统一文案。更根本的做法是在文档中约定「**只以 `error_code` 为准，`msg` 仅供人类阅读**」，并让各语言示例遵循该约定。

---

#### L-04 错误码表存在成段断号

**证据等级**：A（官方文档原文；方法可复现）

**问题**
错误码表在区间内大段缺失，客户端无法穷举错误分支。

**复现步骤**
1. 打开现货文档的 `Error Code` 小节；
2. 把表格第一列的 5 位数字全部抄出（或用浏览器检索 `^1[0-9]{4}`）；
3. 排序后看相邻差值与区间内缺号。

**观察到的行为**
- 表内共 **84 个**码，范围 10000–10801；
- `10039` 之后直接跳到 `10066`（缺 **10040–10065**，26 个号）；
- `10067` 之后直接跳到 `10100`（缺 **10068–10099**，32 个号）；
- 同法还可看到更小的间断：`10115`、`10124`、`10129–10131`、`10204`、`10602–10605` 等。

**影响**
客户端只能写 `default: unknown_error`，任何未列出的码都会落到兜底分支。对自动交易系统而言，这意味着「未知错误」可能同时覆盖「资金不足」「权限不足」等本应区别处理的情形。

**建议修复**
补齐占位说明（哪些区间已废弃、哪些预留）；或提供机器可读的完整码表（JSON/CSV），以便客户端与 SDK 生成。

---

#### L-05 错误码 10014 与 10016 的描述重复

**证据等级**：A（官方文档原文）

**问题**
错误码表中 `10014` 与 `10016` 的描述完全相同（均为 `Currency is not enough`），文档层面两码无区分语义。

**复现步骤**
打开现货文档 `Error Code` 小节，定位 `10014` 与 `10016` 两行，比对描述列。

**观察到的行为**
两行描述逐字相同，均为 `Currency is not enough`。

**影响**
客户端无法判断两码是否对应不同的前置条件（例如「可用余额不足」与「冻结后总额不足」），只能合并处理。

**建议修复**
为两码给出可区分的定义；若无区别，合并为一个码。

---

### B 组｜签名与鉴权文档

#### L-06 合约文档签名示例中 `HmacSHA556` 拼写错误

**证据等级**：A（官方文档原文）

**问题**
合约文档的签名串示例中，签名算法名写成 `HmacSHA556`（**1 处**）；全文另有 **11 处**为正确的 `HmacSHA256`。

**复现步骤**
1. 打开合约文档 `https://www.lbank.info/docs/contract.html` 的签名示例小节；
2. 在页面内检索 `HmacSHA`（或用浏览器「查看页面源代码」后检索）；
3. 统计 `HmacSHA` 之后的数字。

**观察到的行为**
- `HmacSHA256` × **11**；
- `HmacSHA556` × **1**，出现在签名串示例中：`string parameters="...&signature_method=HmacSHA556&timestamp=91654"`。

**影响**
照抄示例的客户端会取到无效的签名算法名，签名请求必然失败，且返回的错误信息不会指向这个拼写 —— 属于可原地消除的集成摩擦。

**建议修复**
改 1 个字符。

---

#### L-07 合约签名示例的时间戳被截断

**证据等级**：A（官方文档原文）

**问题**
同一示例签名串中的 `timestamp` 只有 5 位，而真实毫秒时间戳为 13 位。

**复现步骤**
在合约文档签名小节中，比对「待签名字符串示例」与「提交签名参数」两处的 `timestamp` 取值。

**观察到的行为**
- 待签名字符串示例：`...&timestamp=91654`（**5 位**）；
- 提交参数示例：`...&timestamp=1665990154559`（**13 位**，毫秒）。

**影响**
示例**不可原样复制运行**。与 L-06 叠加后，「照抄示例」这条路径走不通，集成方只能靠试错确定正确形态。

**建议修复**
把示例签名串替换为一条完整、可直接校验的样例；建议同时给出「已知 secret → 已知 sign」的测试向量，这是集成方最省时间的一项补充。

---

### C 组｜时间与粒度语义

#### L-08 日/周/月 K 线桶边界按 UTC+8 对齐，返回的却是 UTC 纪元秒

**证据等级**：A（官方端点实测；算术可复现）

**问题**
`kline.do` 返回的 K 线时间戳是标准 Unix 纪元秒（UTC 语义），但 `day1` 的桶边界按 UTC+8 切分。文档未在 K 线返回说明处声明边界时区。

**复现步骤**
1. 取当前秒级时间戳（或直接使用文档示例中的 `time`）：
   `curl "https://api.lbank.info/v2/kline.do?symbol=btc_usdt&size=3&type=day1&time=1769914800"`
2. 取返回数组首根的第一个元素（桶起始时间戳）；
3. 把该时间戳按 **UTC** 与 **UTC+8** 分别换算，与请求的 `time` 对比；
4. 对照组：`type=minute1` 做同样换算。
   `curl "https://api.lbank.info/v2/kline.do?symbol=btc_usdt&size=3&type=minute1&time=1789984246"`

**观察到的行为（本次观测）**
- 日线（步骤 1）：请求 `time=1769914800` = 2026-02-01 **03:00 UTC** = 2026-02-01 **11:00 UTC+8**；返回首桶 `1769875200` = 2026-01-31 **16:00 UTC** = 2026-02-01 **00:00 UTC+8**。
  → 请求时刻是 UTC+8 的 11:00，返回的第一根日线从 **UTC+8 当日零点**起：**桶边界按 UTC+8 对齐**，而时间戳本身是 UTC 纪元秒。
- 分钟线（步骤 4）：请求 `time=1789984246`（UTC+8 17:50:46）→ 返回首桶 `1789984200`（UTC+8 17:50:00）。
  → 分钟级桶为分钟对齐，**无时区偏移**。

**影响**
跨所对齐日线时，若把 LBank 日线的纪元秒按「UTC 日」解释，会与按 UTC 切桶的交易所错开 8 小时；用「本地日期 → 纪元秒」反推日线索引会取到错误的桶。整个过程不返回任何错误。

**建议修复**
在 K 线返回说明处显式写明「`day1`/`week1`/`month1` 的桶边界为 UTC+8 零点」；或提供 `utcOffset` / `tz` 字段。

**范围限制**
上述桶边界结论限于 `day1`；同族的 `week1`/`month1` **未单独实测**（见 §五）。

---

#### L-09 WS 一次性请求的 `start` 接受无时区 ISO 字符串，文档明示按北京时间解释

**证据等级**：A（官方文档原文）

**问题**
WS 一次性请求参数 `start` 同时接受两种格式，且两种格式的时区语义不同。

**复现步骤**
打开现货文档 `WebSocket API (Market Data)` 小节，定位 `start` 参数的描述行与示例报文。

**观察到的行为**
文档原文（`start` 参数描述）：

> Start time. Accept 2 formats, such as 2018-08-03T17:32:00 (beijing time), another timestamp, such as 1533288720 (Accurate to second)

即：ISO 串按**北京时间**解释，数字按**纪元秒（UTC 语义）**解释。

**影响**
客户端若把本地生成的 `2026-02-01T03:00:00`（本意为 UTC）传入，会被当作北京时间，产生 8 小时偏移；且偏移方向随客户端时区而变。

**建议修复**
只接受带显式时区的 ISO 8601（`Z` 或 `+08:00`）；或统一改为纪元秒。

---

#### L-10 WS 推送的 `TS` 字段无时区标记

**证据等级**：A（官方文档原文）

**问题**
WS 各频道推送均带顶层 `TS`（人类可读字符串，形如 `"2019-06-28T17:49:22.722"`），字段表仅写 `TS | String | Deal time`，未注明时区。同一次推送内还有其它业务时间字段（如 `trade.TS`、`kbar.t`），其来源与时区同样未声明。

**复现步骤**
1. 打开现货文档 `WebSocket API (Market Data)` 的推送样例小节，看 `kbar` 推送样例的顶层 `TS` 取值；
2. 在同一小节的字段表中定位 `TS` 行与描述列。

**观察到的行为**
- 样例取值：`"TS":"2019-06-28T17:49:22.722"`（无 `Z`、无时区偏移）；
- 字段表：`TS | String | Deal time`。

**状态限制**
本次 WS 场次未落盘业务帧的 `TS` 实际取值，因此 **`TS` 的时区归属为「未判定」**；已核实的仅是「无时区标记」这一点（判定方法见 §五）。

**影响**
`TS` 与纪元秒字段混用时，会引入 0 或 8 小时的系统性偏差，且在白天盘中不易被发现（价格看起来是合理的）。

**建议修复**
`TS` 统一为带 `Z` 的 ISO 8601；或在字段表中补充时区说明。

---

#### L-11 REST 的 `type` 与 WS 的 `kbar` 使用两套时间粒度命名

**证据等级**：A（官方文档原文）+ B（第三方开源库源码）

**问题**
同一交易所的两个通道使用两套粒度枚举，无法共用一份本地配置。

**复现步骤**
1. 现货文档 `Query K Bar Data` 小节 → `type` 参数枚举；
2. 现货文档 `WebSocket` 小节 → `kbar` 订阅样例与取值；
3. 对照第三方开源库 CCXT 的 LBank 实现：REST 粒度表位于 `ccxt/pro/lbank.py` 的 `timeframes`，WS 粒度表位于 `ccxt/pro_lbank.py` 的 `options.watchOHLCV.timeframes`（按符号名检索即可定位）。

**观察到的行为**
- REST `type` 枚举 11 个值：`minute1 / minute5 / minute15 / minute30 / hour1 / hour4 / hour8 / hour12 / day1 / week1 / month1`（**无年线**）；
- WS `kbar` 取值：`1min / 5min / 15min / 30min / 1hr / 4hr / day / week / month / year`（**有年线**）；
- CCXT 为 WS 维护了独立映射表：`1m→1min`、`5m→5min`、`15m→15min`、`30m→30min`、`1h→1hr`、`4h→4hr`、`1d→day`、`1w→week`、`1M→month`、`1y→year`。

**影响**
同一份本地配置无法在 REST 与 WS 之间复用；跨通道对齐 K 线时必须维护两套映射（第三方库即因此被迫这么实现）。

**建议修复**
统一命名（例如 REST 与 WS 共用 `1m/1h/1d` 一类写法），或在文档中给出一张两通道的对照表。

---

#### L-12 `kline.do` 的 `time` 为必填参数（已关闭，非缺陷）

**证据等级**：A（官方文档原文 + 官方端点实测）
**定性**：**已关闭，不计为缺陷** —— 文档与实现自洽。保留此条是因其为对接中最常见的参数坑，可作为背景。

**问题**
`kline.do` 的 `time` 参数为必填（文档参数表标注 `Required = Yes`，类型 `String`，说明 `Timestamp (of Seconds)`）；漏传即报 `10003`。

**复现步骤**
1. 文档原样 URL：`curl "https://api.lbank.info/v2/kline.do?symbol=btc_usdt&size=3&type=day1&time=1769914800"`
2. 去掉 `time`：`curl "https://api.lbank.info/v2/kline.do?symbol=btc_usdt&size=3&type=minute1"`

**观察到的行为（本次观测）**
- 步骤 1 → HTTP 200，`error_code 0`，返回 3 根 K 线（**文档示例可原样复制运行**）；
- 步骤 2 → HTTP 200，`error_code 10003`。

**影响**
漏传 `time` 只会得到 `10003 Illegal parameter`，错误信息不指向缺失的参数名，容易被误判为「端点故障」或「文档与实现不一致」（我们自己的初版核对就曾因此把该端点列为疑似契约差异）。文档与实现本身是自洽的，本条不计为缺陷。

**建议修复（可选，仅改善集成摩擦）**
参数表对必填项做显著标注；错误信息中提示缺失的参数名。

---

### D 组｜错误码与限流

#### L-13 限流章节仅两条全局限额，缺少配额表与 `429` 语义

**证据等级**：A（官方文档原文）

**问题**
限流章节只给出两条全局额度，**公开层面未见**分端点（或按权重）配额、限流维度、剩余额度回传、`429`/`Retry-After` 语义与超限后的封禁策略。

**复现步骤**
打开现货文档 `Endpoint Rate Limit` 小节，读其全部正文。

**观察到的行为**
该小节正文仅两条：

> Create order and cancel order Request 500/10s
> The other Request 200/10s

公开层面未见：按端点的 weight 表、限流维度（IP / UID / api_key）、额度查询端点、`429` / `Retry-After` 说明、超限后的封禁或降级策略。

**影响**
客户端只能在「打到报错」时才知道边界；缺少 `Retry-After` 时，客户端只能按自行假设的节奏退避，高频场景下容易从「被限流」滑向「被封禁」。对做市/信号类客户端，这是一个可观测性缺口，而非单纯的文档瑕疵。

**建议修复**
1. 给出分端点（或按权重）的配额表；
2. 超限时返回**明确的 HTTP 状态码与 `Retry-After`**；
3. 在响应头回传剩余配额；
4. 提供查询当前配额的端点。

**对照说明**
部分主流交易所公开了更细的限流细则与响应头约定。本清单**未在本次测量中复核**这些对照项，故列为「待公开化」（附录 C），不在此处作事实断言。

---

#### L-14 公开层面未见测试环境说明

**证据等级**：A（官方文档原文）+ B（第三方开源库源码）

**问题**
公开文档中**未见**独立 testnet / sandbox 域名与测试资产说明。

**复现步骤**
1. 在现货文档与合约文档中检索 `test` / `sandbox` / `testnet`，查看命中的上下文；
2. 查看第三方开源库 CCXT 的 LBank 实现中 `sandbox` 相关配置（`ccxt/pro/lbank.py`，按符号名检索）。

**观察到的行为**
- 现货文档仅有一处 `POST /v2/supplement/create_order_test.do`，文档未说明其作用范围；
- 合约文档有一节 `test account`，给出的是**明文示例凭据**（形如 `String privateKey = "…"`），更接近签名演示而非可用环境；
- CCXT 的 LBank 实现中 `'sandbox': False`。

**措辞限制**
本清单只主张「**公开层面未见**可用测试环境说明」。是否对机构客户提供独立环境，属我们不可见的范围，本清单不作推断。

**影响**
在缺少公开测试环境的情况下，下单、撤单、重连、风控逻辑的改动通常只能上主网验证，这是集成方常提到的接入摩擦之一。

**建议修复**
公开 testnet 域名与测试资产；或在文档中明确 `create_order_test.do` 的语义与适用范围。

---

### E 组｜端点与路径的文档完整性

#### L-15 若干仍在使用的公开端点未文档化

**证据等级**：A（官方文档检索）+ B（第三方开源库源码）

**问题**
以下端点仍在被第三方库使用（部分实测可用），但官方文档**无参数表、无返回说明**：`/v2/usdToCny.do`、`/v2/incrDepth.do`、`/v2/trades.do`（不带 `supplement/` 前缀）、`/v2/ticker.do`。

**复现步骤**
在现货文档全文检索以下字符串并记录命中位置与上下文：`usdToCny`、`incrDepth`、`trades.do`、`ticker.do`。

**观察到的行为**
- `usdToCny` → 0 命中；`incrDepth` → 0 命中；
- `ticker.do` → 仅 **2 处**命中，均在 changelog：一处是「建议以 `ticker/24hr.do` 替换 `ticker.do`」，另一处是 2022.11.03 的 Update 条目；两处**均无参数表、无返回说明**；
- 第三方开源库 CCXT 仍将 `usdToCny`、`incrDepth`、`trades`、`ticker` 列为 public 端点。

**影响**
第三方库依赖**未文档化**端点，意味着契约无承诺：随时可能被下架而无人收到通知。

**建议修复**
补文档；或将其列入文档已有的 `Abandoned Endpoints` 一节并给出弃用时间表（该节当前仅 1 条）。本清单不主张必须保留这些端点，主张的是**契约要公开**。

---

#### L-16 第二个现货 REST 域名 `api.lbkex.com` 未在文档中提及

**证据等级**：A（官方端点实测 + 官方文档检索）

**问题**
`https://api.lbkex.com/` 与文档声明的主域名 `https://api.lbank.info/` 在功能上等价，但文档全文未提及前者。

**复现步骤**
1. `curl "https://api.lbank.info/v2/kline.do?symbol=btc_usdt&size=3&type=minute1&time=<当前秒级时间戳>"`
2. 把同一 URL 的域名换成 `https://api.lbkex.com/` 再请求一次；
3. 比对两条响应的 K 线数据（时间戳、价格、数量）；
4. 在现货文档全文检索 `lbkex`。

**观察到的行为（本次观测）**
- 两次请求的返回数据**逐字节一致**（同毫秒、同价、同量）；
- `lbkex` 在文档全文 **0 命中**。

**影响**
开发者无法从文档判断第二个域名是否受支持、是否有 SLA、是否将长期保留；多域名也意味着「故障切换」与延迟对比的口径必须自行摸索。

**建议修复**
文档列出全部受支持域名及其关系（主域名 / 备用 / 别名），并说明是否共享同一契约与配额。

**限制**
本条为单次同参对照（n=1），只主张「该次请求下两者返回一致」。

---

#### L-17 WS 接入域名在文档与第三方实现之间不一致

**证据等级**：A（官方文档原文）+ B（第三方开源库源码）

**问题**
文档声明现货 WS 为 `wss://api.lbank.info/ws/V2/`，而第三方开源库 CCXT 的 WS 实现指向另一个域名 `wss://www.lbkex.net/ws/V2/`；后者在文档中未被提及。

**复现步骤**
1. 现货文档 `WebSocket` 小节 → 接入地址；
2. 第三方开源库 CCXT `ccxt/pro_lbank.py` → `urls.api.ws`（按符号名检索）；
3. 在现货文档全文检索 `lbkex.net`。

**观察到的行为**
- 文档：`wss://api.lbank.info/ws/V2/`；
- CCXT：`'ws': 'wss://www.lbkex.net/ws/V2/'`；
- `lbkex.net` 在文档全文 **0 命中**。

**影响**
两条不同的边缘路径可能带来不同的限流口径、行为与统计，跨客户端的结果不可直接比较（我们自己的测量因此必须固定域名）。

**建议修复**
文档列出全部可用的 WS 接入域名；若 `www.lbkex.net` 已废弃，请在 changelog 给出下线通知（当前第三方库仍在使用它）。

---

#### L-18 粒度 `hour2` / `hour6` 是否可用 —— 未判定

**证据等级**：A（官方文档枚举）+ B（第三方开源库源码）
**状态**：**未判定，本清单不下结论。**

**问题**
第三方开源库 CCXT 的 REST 粒度声明中含 `2h→hour2`、`6h→hour6`（`ccxt/pro/lbank.py` 的 `timeframes`），而文档 `type` 的 11 个枚举值中**不含** `hour2`/`hour6`。两者不一致，但**本清单不对哪一方正确下结论**。

**复现步骤**
1. 打开现货文档 `Query K Bar Data` 小节，读 `type` 参数的枚举值；
2. 到第三方开源库 CCXT 的 `ccxt/pro/lbank.py` 中检索 `timeframes`，读其取值；
3. 比对两侧是否都含 2 小时 / 6 小时粒度。

**观察到的行为**
- 文档枚举 11 个值，**不含** `hour2` / `hour6`；
- CCXT 声明含 `2h→hour2`、`6h→hour6`；
- 我们**未**对 `type=hour2` 发起实测（本次测量未覆盖该参数）。

**影响**
现状下开发者无法从文档确定 2 小时 / 6 小时 K 线是否可用：写入配置可能得到 `10003`，不写入则可能放弃本可用的粒度。

**建议修复**
补充文档枚举（若确实支持）；或在 `type` 参数表注明「仅支持下列取值」并说明其它取值的返回行为。本清单的判定方法如下。

**为什么未判定**
两种解释都不能排除：可能是「文档枚举不完整」，也可能是「第三方库声明过宽」。仅凭文档与源码无法区分。

**判定方法（单次请求即可）**
```
curl "https://api.lbank.info/v2/kline.do?symbol=btc_usdt&size=2&type=hour2&time=<当前秒级时间戳-7200>"
```
- 返回 `error_code 0` → 文档枚举不完整（转「文档缺」）；
- 返回 `error_code 10003` → 第三方库声明过宽（文档正确）。

---

#### L-19 `etfTicker/24hr.do` 必填 `symbol`，但文档未给出杠杆代币交易对的枚举途径

**证据等级**：A（官方文档原文 + 官方端点实测）

**问题**
`etfTicker/24hr.do` 的 `symbol` 为必填；文档在说明「不含杠杆代币」时给出的取法**指回该端点自身**，且未给出 ETF 交易对的合法来源。

**复现步骤**
1. `curl "https://api.lbank.info/v2/etfTicker/24hr.do"`（不带 `symbol`）；
2. 打开现货文档 `24hr Ticker` 小节，读该小节正文括号内的说明。

**观察到的行为**
- 步骤 1 → HTTP 200，`error_code 10001`，`msg "Parameter can not be null"`（与文档 `symbol` 必填一致）；
- 文档 `24hr Ticker` 小节正文原文：

> GET the LBank coin quote data, excluding Leveraged Tokens trading pairs (get Leveraged Tokens trading pairs GET /v2/etfTicker/24hr.do)

即：其中给出的「取杠杆代币交易对」的方法，就是调用 `etfTicker/24hr.do` 本身；而该端点又必须带 `symbol`。文档**未给出 ETF 交易对的枚举来源**（`currencyPairs.do` 是否包含 ETF 对，文档也未表态）。

**影响**
开发者知道存在 ETF 行情端点，但不知道合法 `symbol` 从哪里来，只能试错。

**建议修复**
在 `etfTicker/24hr.do` 小节说明 ETF 交易对的来源端点，以及 `symbol=all` 的语义。

---

#### L-20 WS 文档未覆盖订阅上限、压缩、重连与丢帧可检测性

**证据等级**：A（官方文档检索）

**问题**
文档未给出单连接订阅条目上限、消息数上限、连接有效期、丢帧可检测性（序号/校验和）、重连与补发语义、压缩支持情况。

**复现步骤**
对现货文档与合约文档全文做**不区分大小写**检索，统计以下关键词命中数：`compress`、`permessage`、`gzip`、`reconnect`、`sequence`、`seq`、`checksum`、`shutdown`。

**观察到的行为**
- 上述 8 个关键词在两份文档中的命中数**均为 0**；
- `subscri` 在有订阅说明的文档中命中 **58** 次，但未出现与 `limit`/`max` 同现的上限规定；
- 文档给出的连接存活约束只有一条（原文）：客户端在 1 分钟内未回应服务端 Ping，服务端即关闭连接。

**影响**
客户端需要自行设计重连与背压；文档未提供序号或校验和机制，因此丢帧难以由协议字段直接识别 —— 本地订单簿的完整性需要客户端自行推断（例如定期快照重建或比对业务字段），做市类客户端的错价风险由客户端承担。

**建议修复**
公开订阅数 / 消息数 / 连接数上限与连接有效期；在推送中加入序号或校验和；说明重连后是「补发」还是「跳最新」；提供服务端主动下线（如 `serverShutdown` 类事件）的提前通知。

---

#### L-21 本次观测：现货 WS 握手未协商 `permessage-deflate`

**证据等级**：A（官方端点实测，**n=1**）

**问题**
2026-09-21 的单次 WS 握手响应头中未出现 `Sec-WebSocket-Extensions`，即本次观测未协商压缩扩展。

**复现步骤**
1. 用任意 RFC6455 WebSocket 客户端连接 `wss://api.lbank.info/ws/V2/`；
2. 记录握手响应头全文；
3. 查看是否存在 `Sec-WebSocket-Extensions` 行。

**观察到的行为（本次观测）**
- 握手返回 `HTTP/1.1 101 Switching Protocols` 与 `Sec-WebSocket-Accept: 8Io10aL1YgoXzF/gH4kWHT3yoIE=`；
- **未出现** `Sec-WebSocket-Extensions` 响应头。

**措辞限制（重要）**
n=1，只能写「**本次观测未协商**」。**不能**据此推断「服务端不支持压缩」：判断服务端是否支持，需要客户端**主动 offer** 该扩展后再观察响应头。本清单未做该主动 offer 测试（判定方法见 §五）。**该主动 offer 测试已于 2026-09-22 补做**，结论见 `examples/02-permessage-deflate-negotiation.md` 与 `examples/03-lbank-deflate-ladder-probe.json`：LBank 对三档 offer 变体（含 `client_max_window_bits` 与 `server_max_window_bits=15` 变体）**全部拒绝**，`deflate_status = server_refused`。即：本条**当时**的「未协商」已被后续实验收窄为「服务端拒绝」，不再是悬置项。

**影响（条件性）**
若服务端支持而客户端未 offer，该差异可由客户端修复消除；若服务端不支持，则高频成交流的线级字节数会明显高于支持压缩的同类接口。

**建议修复**
文档明确是否支持 `permessage-deflate`；若支持，给出启用方式。

**附（同次连接内的其它观测）**
主动发送 `{"action":"ping","ping":"<uuid>"}` 后**立即**收到 `{"action":"pong","pong":"<同一 uuid>"}`；订阅 `tick` 频道后 8 秒内收到 **107 条** `tick` 推送（另有 1 条上述 pong 帧）。该连接**无需任何密钥**。

---

### F 组｜仓库与门户运维（非契约）

#### L-22 搜索引擎索引中的「官方文档仓库」返回 404

**证据等级**：A（本次观测）

**问题**
搜索结果中出现的 `github.com/LBank-exchange/lbank-official-api-docs` 返回 **404**。

**复现步骤**
1. 打开 `https://github.com/LBank-exchange/lbank-official-api-docs`；
2. 用 GitHub API 列出 `LBank-exchange` 账号下的公开仓库。

**观察到的行为（本次观测）**
- 该仓库返回 404；
- 同一账号下现存 **4 个**仓库，全部为 connector 类型。

**影响**
开发者按搜索索引寻找「官方文档仓库」会撞 404，降低对文档权威性的信任；同时缺少 issue 反馈通道。

**建议修复**
恢复该仓库（或放一个 README 指回官网文档），并提供明确的文档反馈通道（issue 模板或邮箱）。

---

#### L-23 官方文档门户 `www.lbank.com/docs/index.html` 返回地区限制页

**证据等级**：A（本次观测）
**定性**：**可达性问题，非契约缺陷。**

**问题**
`https://www.lbank.com/docs/index.html`（含 `/zh-CN/docs/`）返回 **403** 地区限制页。

**复现步骤**
1. `curl -i "https://www.lbank.com/docs/index.html"`；
2. 打开 `https://www.lbank.info/docs/index.html` 对比内容。

**观察到的行为（本次观测）**
- 步骤 1 → HTTP **403**，页面文案 `LBank is currently unavailable in your country or region`；
- 步骤 2 → HTTP 200，页面标题同为 `Introduction – LBank API`，正文段落与门户版逐字一致。

**影响**
文档可达性随出口地区而变，第三方引用文档时难以给出稳定 URL（我们因此以 `lbank.info` 为准）。这对开发者社区与 AI 训练语料的稳定引用不利。

**建议修复**
文档页不套用地区策略（或提供一个 region-neutral 的文档域名）；并说明文档入口与业务门户的地区策略差异。

**声明**
我们**未**尝试任何绕过访问控制的手段，只是改用官方另一入口。

---

## 四、建议优先序

按「对开发者的代价」排序，供贵方参考（非约束性）。

| 优先 | 条目 | 理由 |
|---|---|---|
| 1 | L-08 / L-09 / L-10（时间戳与时区语义） | 不返回错误、只影响数值语义：按错误时区解释会静默错 8 小时 |
| 2 | L-13（限流语义） | 决定客户端能否实现正确的退避与背压；缺此说明即构成可观测性缺口 |
| 3 | L-01 / L-02（响应信封类型） | 强类型客户端会解析失败，或以 `msg` 误判成功 |
| 4 | L-14（测试环境） | 决定「改动能否在不使用真实资金的前提下验证」 |
| 5 | L-20 / L-21（WS 上限 / 序号 / 压缩） | 决定客户端能否自建可验证的本地簿与背压 |
| 6 | L-06 / L-07（签名示例） | 改动量极小，收益立即 |
| 7 | L-03 / L-04 / L-05（错误码表） | 机器可读码表一次投入、长期受益 |
| 8 | L-15 ~ L-19、L-22、L-23（文档完整性与可达性） | 影响接入摩擦与社区信任 |

---

## 五、未判定（本清单不下结论）

| 项 | 状态 | 判定所需的单次操作 |
|---|---|---|
| WS 是否支持 `permessage-deflate` | 本清单截至 2026-09-21 **未判定**（仅观测到本次未协商）；**2026-09-22 已补测 → `server_refused`**（见 `examples/02`、`examples/03`） | 客户端**主动 offer** 该扩展，看响应头是否回该扩展 |
| `type=hour2` / `hour6` 是否可用 | 未判定 | `curl "…/kline.do?symbol=btc_usdt&size=2&type=hour2&time=<now-7200>"` |
| `day1` 之外的 `week1`/`month1` 桶边界 | 未判定（仅 `day1` 有实测） | 同法请求 `type=week1` / `type=month1`，比对首桶日期 |
| `TS` 字段的实际时区归属 | 未判定（仅「无时区标记」已核实） | 抓一条业务推送，与 `timestamp.do` 的返回值对照 |
| 合约 WS `wss://lbkperpws.lbank.com/ws` 的行为 | 未实测 | 单次握手 + 订阅 |
| 合约 `instrument` 的 `productGroup` 合法枚举 | 未判定（传 `productGroup=1` 返回空数组） | 用文档示例值 `SwapU` 复测 `instrument` / `marketData` |
| 现货 WS 订阅条目上限 | 未测定 | 需多订阅分档探测；超出本次「单次低频」纪律，建议以工单询问 |
| `429` 与真实限流行为 | 未测定（纪律原因） | 需官方书面许可 + 隔离环境 |

---

## 附录 A：测量方法与环境

| 项 | 内容 |
|---|---|
| 日期时间 | 2026-09-21（UTC+8） |
| 出口 | 本机配置的 HTTP(S) 代理（非中国大陆出口）。文档要求以 non-China IP 发起调用。**具体出口 IP 本次未留档**，故涉及出口的结论（L-23）只作「本次观测」表述 |
| 文档来源 1 | `https://www.lbank.info/docs/index.html` → HTTP 200；正文副本 **300,374 字节 / 4,697 行**（现货文档） |
| 文档来源 2 | `https://www.lbank.info/docs/contract.html` → HTTP 200；**44,767 字节**（合约文档） |
| 总访问次数 | **20 次**（19 次 HTTP + 1 次 WS 连接），请求间隔 ≥1.2 秒，**无并发 / 无重试 / 无扫描 / 无压测** |
| 样本量 | 逐字文档副本 2 份；公开端点 19 次；WS 会话 1 次（8 秒） |
| 工具 | `curl`、Python `requests`、标准 RFC6455 WebSocket 客户端、浏览器直接打开文档页 |
| 证据等级 | A（官方文档原文 / 官方端点实测）、B（第三方公开来源）；C 未使用 |
| 未触碰 | 任何签名接口、账户接口、交易接口、资金接口；未做压测；未绕过任何访问控制 |

**19 次 HTTP 访问的端点清单（可逐条复现）**

| # | 端点 / 参数 | 结果 |
|---|---|---|
| 1 | `GET /v2/kline.do?symbol=btc_usdt&size=3&type=day1&time=1769914800`（文档原样） | 200，`error_code 0`，返回 3 根日线 |
| 2 | `GET /v2/kline.do?symbol=btc_usdt&size=3&type=minute1&time=1789984246` | 200，`error_code 0`，3 根 1 分钟线 |
| 3 | `GET /v2/kline.do?symbol=btc_usdt&size=3&type=minute1`（无 `time`） | 200，`error_code 10003`，`msg "Illegal parameter"` |
| 4 | 同 #2，域名改 `api.lbkex.com` | 200，`error_code 0`，与 #2 **逐字节一致** |
| 5 | `GET /v2/supplement/ticker/price.do?symbol=btc_usdt` | 200，`result` 为**布尔** `true`，`msg` 为 `null` |
| 6 | `GET /v2/supplement/ticker/bookTicker.do?symbol=btc_usdt` | 200，`result` 为字符串 `"true"` |
| 7 | `GET /v2/ticker/24hr.do?symbol=btc_usdt` | 200，`result` 为字符串 `"true"` |
| 8 | `GET /v2/etfTicker/24hr.do`（无 `symbol`） | 200，`error_code 10001`，`msg "Parameter can not be null"` |
| 9 | `GET /v2/supplement/trades.do?symbol=btc_usdt&size=3` | 200，`error_code 0` |
| 10 | `GET /v2/depth.do?symbol=btc_usdt&size=5` | 200，`error_code 0` |
| 11 | `GET /v2/currencyPairs.do` | 200，返回 **1361** 个交易对 |
| 12 | `GET /v2/accuracy.do` | 200，返回 **1361** 条精度记录 |
| 13 | `GET /v2/assetConfigs.do`（无 `assetCode`） | 200，`error_code 10001` |
| 14 | `GET /v2/timestamp.do` | 200，毫秒时间戳 |
| 15 | `POST /v2/supplement/system_ping.do`（无参数） | 200，`data {}`，`msg` 为 `null` |
| 16 | `GET https://lbkperp.lbank.com/cfd/openApi/v1/pub/getTime` | 200，`error_code 0` |
| 17 | `GET .../cfd/openApi/v1/pub/instrument?productGroup=1` | 200，但 `data` 为空数组（见 §五） |
| 18 | `GET .../cfd/openApi/v1/pub/marketOrder?symbol=BTCUSDT&depth=5` | 200，返回 `symbol/asks/bids` |
| 19 | `GET https://api.lbank.com/`（连通性检查） | 连接被重置（该域名无服务；不计入问题清单） |

**1 次 WS 会话**

- 目标：`wss://api.lbank.info/ws/V2/`（经同一出口代理）；
- 握手：`HTTP/1.1 101 Switching Protocols`，`Sec-WebSocket-Accept: 8Io10aL1YgoXzF/gH4kWHT3yoIE=`，无 `Sec-WebSocket-Extensions`；
- 发送：主动 `{"action":"ping","ping":"<uuid>"}` + 订阅 `{"action":"subscribe","subscribe":"tick","pair":"btc_usdt"}`；
- 接收：8 秒内 108 帧（1 帧 pong + 107 帧 `tick`）；
- 结束：单连接，未重连。

---

## 附录 B：措辞与证据等级说明

本清单在表述上遵循以下约定，目的是让读者能区分**事实**与**判断**：

| 用语 | 含义 |
|---|---|
| 「官方文档原文」 | 直接引用官方文档的句子或表格，读者可在文档页逐字比对 |
| 「本次观测」 | 我们在 2026-09-21 的**单次**请求中观察到的结果；不作为普遍性定性 |
| 「公开层面未见」 | 在**我们检索的公开材料范围内**未发现；不主张其不存在（机构或私有材料不在我们可见范围） |
| 「未判定」 | 现有材料不足以区分多种解释，本清单不下结论，并给出判定方法 |
| 「0 命中」 | 在指定的**两份官方文档全文**、不区分大小写的检索结果，方法已在原文给出，可复现 |
| 证据等级 A / B / C | 分别为「官方文档原文或官方端点实测」/「第三方公开来源」/「自媒体与聚合站线索（本次未使用）」 |

**我们避免的表述**：不对交易所作整体评价（如「质量差」「不专业」等），不使用无样本量支撑的绝对化断言。负面结论一律改为「公开层面未见」等可界定的写法。

---

## 附录 C：引用形式的公开化处理

下列引用形式需替换为读者可核验的形式，处理建议如下：

| # | 本版现状 | 待公开化的内容 | 处理建议 |
|---|---|---|---|
| C-1 | 文档原文引用以**小节名**定位 | 我们逐字文档副本中的**行号**（用于精确定位，如错误码表行号、参数描述行号） | 随文档副本一并公开，或改为「小节名 + 引文片段」形式（**本版已按后者写作**） |
| C-2 | 端点实测结果以**请求 + 字段**描述 | 原始 HTTP/WS 响应全文与握手头存档 | 脱敏后随清单附件公开 |
| C-3 | 第三方库引用以**符号名**定位（如 `ccxt/pro/lbank.py` 的 `timeframes`、`pro_urls.api.ws`） | 具体版本号 / commit 号 | 发布前补记版本，供读者固定复现 |
| C-4 | ~~§四 的 3 条第三方口径~~ | 该机构级连接器文档的**可公开出处** | **已处理（2026-09-23）：整节移除。** 该来源无公开出处，保留即构成无出处断言；所涉 3 条本就标注「未独立复现」，删除不损失任何 A 级结论，故未纳入本清单的编号条目数 |
| C-5 | L-13 的行业对照项 | 对照交易所的限流细则与响应头约定（本次**未复核**） | 若保留对照，需逐条实测复核后再写入；否则保持现状（仅述「部分交易所有更细公开细则」） |
| C-6 | 附录 A 的「出口 IP 未留档」 | 出口 IP / ASN | 如需要，可补记；本版明确说明该项未留档，相关结论只作「本次观测」 |

---

**本文件状态**：公开稿。本文只涉及公开文档与公开行情端点，未使用任何签名、账户、交易、资金接口，未做压测、扫描或访问控制绕过。
